// editorjs-list.d.ts

declare module '@editorjs/list' {
    // If the module exports a default value, you can specify its type
    import { SomeType } from 'some-module'; // Adjust this import based on the actual module used
    const EditorJSList: SomeType; // Adjust SomeType based on the actual type exported by the module
    export default EditorJSList;

    // You can also declare other exports of the module if needed
}

declare module '@editorjs/paragraph' {
    // If the module exports a default value, you can specify its type
    import { SomeType } from 'some-module'; // Adjust this import based on the actual module used
    const EditorJSList: SomeType; // Adjust SomeType based on the actual type exported by the module
    export default EditorJSList;

    // You can also declare other exports of the module if needed
}

declare module '@editorjs/quote' {
    // If the module exports a default value, you can specify its type
    import { SomeType } from 'some-module'; // Adjust this import based on the actual module used
    const EditorJSList: SomeType; // Adjust SomeType based on the actual type exported by the module
    export default EditorJSList;

    // You can also declare other exports of the module if needed
}

declare module '@editorjs/table' {
    // If the module exports a default value, you can specify its type
    import { SomeType } from 'some-module'; // Adjust this import based on the actual module used
    const EditorJSList: SomeType; // Adjust SomeType based on the actual type exported by the module
    export default EditorJSList;

    // You can also declare other exports of the module if needed
}